import React, {Component, PropTypes as T} from 'react';

export class Home extends Component {
  render = () => {
    return (
      <div>
        <h1>Logged In</h1>
      </div>
    );
  }
}

export default Home;
